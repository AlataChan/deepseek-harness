# Keyless overlay for Extension Development Host lifecycle coverage. Model
# output belongs to the assembled snapshot, so this lane adds no test plugin.
- id: llm-deepseek
  name: '@deepseek-ai/dsh-llm-deepseek'
  disabled: true

- id: session-title-llm
  name: '@deepseek-ai/dsh-session-title-first-prompt-llm'
  disabled: true
