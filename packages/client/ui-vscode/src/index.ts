/**
 * VS Code UI plugin node half. The browser half owns all behavior and is
 * discovered through the package's `dsh.client` manifest.
 */

/** Host plugin body; this surface package has no Host behavior. */
export function apply(): void {}
