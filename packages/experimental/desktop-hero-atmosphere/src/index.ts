/**
 * Host half of the desktop Hero atmosphere plugin. The empty apply gives
 * Loader a host-side row while the browser half ships through `./client`.
 * @module @deepseek-ai/dsh-experimental-desktop-hero-atmosphere
 */

/** Host plugin body — this package contributes browser presentation only. */
export function apply(): void {}
