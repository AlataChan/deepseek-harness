# Overlay row for the octopus_DSH desktop Hero atmosphere plate. Dual-face:
# this Host insert is the only patch line; the Client half is discovered from
# `dsh.client` after the Host fiber is live. Do not add a second insert.

- insert:
    - id: desktop-hero-atmosphere
      name: '@deepseek-ai/dsh-experimental-desktop-hero-atmosphere'
