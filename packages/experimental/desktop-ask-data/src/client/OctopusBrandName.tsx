/**
 * Sidebar product name. Occupying this slot drops the official local-build
 * title and the dirty version pill.
 */

/**
 * Render the fork product name.
 * @returns the octopus_DSH wordmark text.
 */
export function OctopusBrandName() {
  return <span>octopus_DSH</span>
}
