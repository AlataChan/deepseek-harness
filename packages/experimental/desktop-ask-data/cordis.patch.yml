# Overlay row for octopus_DSH 问数. Dual-face: this Host
# insert is the only patch line; the Client half is discovered from
# `dsh.client` after the Host fiber is live. Do not add a second insert.

- insert:
    - id: desktop-ask-data
      name: '@deepseek-ai/dsh-experimental-desktop-ask-data'
